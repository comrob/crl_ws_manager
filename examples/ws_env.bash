# ws_manager — example local environment file.
#
# Copy to ~/.config/crl_ws_manager/ws_env.bash and adjust as needed.
# This file is sourced before ws_config.bash, so exported values here are
# available to workspace discovery and command execution.

# ROS environment / distributed compute variables.
# export ROS_DOMAIN_ID=0
# export RMW_IMPLEMENTATION=rmw_zenoh_cpp
# export DIST_COMPUTE_TOKEN=ready
# source /opt/ros/jazzy/setup.bash
